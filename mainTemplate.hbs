{{> header}}
{{#if noteGroups}}
{{#each noteGroups}}

### ⚠️ {{title}} ⚠️

{{#each notes}}
* {{#if commit.scope}}**{{commit.scope}}:** {{/if}}{{text}}
{{/each}}
{{/each}}
{{/if}}
{{#each commitGroups}}

{{#if title}}
    {{#eq title "Features"}}
        ### 💡 {{title}} 💡
    {{/eq}}
    {{#eq title "Patches"}}
        ### 🛠️ {{title}} 🛠️
    {{/eq}}
{{/if}}
{{#each commits}}
{{> commit root=@root}}
{{/each}}
{{/each}}